name: Omanome CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  polish-regression-safety:
    name: "Polish & regression safety"
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run the aggregate 1.4 polish gate
        run: make polish-check

      - name: Verify executable modes in the Git tree
        run: |
          python3 - <<'PY'
          import pathlib
          import subprocess

          expected = []
          errors = []
          for line in subprocess.check_output(["git", "ls-files", "-s"], text=True).splitlines():
              mode, remainder = line.split(" ", 1)
              path = remainder.split("\t", 1)[1]
              rel = pathlib.PurePosixPath(path)
              should_execute = rel == pathlib.PurePosixPath("cli/omanome") or (
                  rel.parts and rel.parts[0] == "input" and rel.suffix == ".sh"
              ) or (
                  rel.parts and rel.parts[0] == "scripts" and rel.suffix == ".py"
              )
              if should_execute:
                  expected.append(path)
              if should_execute and mode != "100755":
                  errors.append(f"{path}: expected 100755, found {mode}")
              if not should_execute and mode == "100755":
                  errors.append(f"{path}: unexpected executable mode")
          if errors:
              raise SystemExit("\n".join(errors))
          print(f"verified {len(expected)} executable files")
          PY

      - name: Run deterministic polish contract tests
        run: python3 -m unittest tests.test_polish -v

      - name: Run 1.4 lifecycle and surface stress fixture
        run: make integration-fixture

  portable:
    name: Portable validation
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Validate JSON documents
        run: |
          python3 -m json.tool manifest.json >/dev/null
          python3 -m json.tool config/defaults.json >/dev/null
          python3 -m json.tool config/schema.json >/dev/null

      - name: Validate project
        run: make validate

      - name: Run project tests
        run: python3 -m unittest tests.test_diagnostics -v

      - name: Run native input contract tests
        run: python3 -m unittest tests.test_input_protocol -v

      - name: Run performance safety tests
        run: python3 -m unittest tests.test_performance_safety -v

      - name: Run process policy tests
        run: python3 -m unittest tests.test_process_policy tests.test_process_watchdog -v

      - name: Run project contract tests
        run: python3 -m unittest tests.test_project -v

      - name: Run configuration migration tests
        run: python3 -m unittest tests.test_config_migration -v

      - name: Run update lifecycle tests
        run: python3 -m unittest tests.test_update_lifecycle -v

      - name: Run QML lint
        run: make qmllint

      - name: Run portable capability and safety checks
        run: |
          make hardware-test
          make dependency-audit
          make performance-test
          make performance-check
          make version-check
          make adaptive-check

      - name: Verify safety invariants
        run: |
          ! grep -RIn --exclude-dir=.git --exclude='*.md' 'xdotool\|gnome-shell[[:space:]]*--replace' .
          ! grep -RIn --exclude-dir=.git --exclude='*.md' 'pkill\|killall' shell input cli
          test -x input/force-quit.sh
          grep -Fq 'companion_pending_marker' cli/omanome
          python3 - <<'PY'
          import json
          manifest = json.load(open("manifest.json", encoding="utf-8"))
          assert "bar" not in manifest["kinds"]
          assert manifest["version"] == "1.4.0"
          PY

  companion-contract:
    name: Optional companion contract
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Validate companion without a desktop session
        run: |
          python3 -m json.tool hypr/omanome-hypr/compatibility.json >/dev/null
          test -f hypr/omanome-hypr/omanome-hypr.cpp
          test -f hypr/omanome-hypr/wobbly-effect.cpp
          test -f hypr/omanome-hypr/wobbly-effect.hpp
          make -C hypr/omanome-hypr test
          grep -Fq '__hyprland_api_get_hash' hypr/omanome-hypr/omanome-hypr.cpp
          grep -Fq '__hyprland_api_get_client_hash' hypr/omanome-hypr/omanome-hypr.cpp
          grep -Fq 'IWindowTransformer' hypr/omanome-hypr/wobbly-effect.cpp
          grep -Fq 'glClearColor(0.F, 0.F, 0.F, 0.F)' hypr/omanome-hypr/wobbly-effect.cpp
          ! grep -RIn 'LD_PRELOAD\|dlsym\|dlopen\|mprotect\|/proc/.*mem' hypr/omanome-hypr
          if pkg-config --exists hyprland; then
            make -C hypr/omanome-hypr check
          else
            echo 'Hyprland development headers are not installed on this runner; build remains an explicit local/manual gate.'
          fi

  performance-safety:
    name: Performance safety
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run static performance gate and owner-only process fixture
        run: |
          make performance-check
          python3 -m unittest tests.test_performance_safety -v
          python3 scripts/process_snapshot.py --json --sample-ms 0

  native-input:
    name: Native input helper
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install Wayland and xkbcommon development files
        run: |
          # The hosted image may contain unrelated third-party sources (for
          # example Chrome) whose transient index can fail independently of
          # this project. Native checks need only Ubuntu packages.
          if [ -f /etc/apt/sources.list.d/ubuntu.sources ]; then
            sudo apt-get \
              -o Dir::Etc::sourcelist="sources.list.d/ubuntu.sources" \
              -o Dir::Etc::sourceparts="-" \
              -o APT::Get::List-Cleanup="0" update
          else
            sudo apt-get update -o Acquire::Retries=3
          fi
          sudo apt-get install -y libwayland-dev libxkbcommon-dev pkg-config

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run native input contract and build gates
        run: make input-check

      - name: Smoke-test metadata mode without a desktop session
        run: |
          ./input/omanome-input/target/release/omanome-input --version
          ./input/omanome-input/target/release/omanome-input --protocol

  multitasking-safety:
    name: Multitasking safety
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run bounded multitasking models and fixture gate
        run: make multitasking-check

      - name: Recheck dependency safety boundary
        run: make dependency-audit

  adaptive-safety:
    name: Adaptive mode safety
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run adaptive device, transition, and ownership gate
        run: make adaptive-check

      - name: Recheck standard bar and hotplug boundaries
        run: |
          python3 - <<'PY'
          import json
          manifest = json.load(open("manifest.json", encoding="utf-8"))
          assert "bar" not in manifest["kinds"]
          assert "bar-widget" in manifest["kinds"]
          assert "barWidget" in manifest["entryPoints"]
          PY
          ! grep -RIn --exclude-dir=.git --exclude='*.md' 'gnome-shell[[:space:]]*--replace\|pkill\|killall' shell input cli

  device-topology-safety:
    name: Device topology safety
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Run graph, mapping, calibration, and topology gate
        run: make device-check

      - name: Recheck device privacy and standard bar boundaries
        run: |
          python3 -m unittest tests.test_device_graph tests.test_calibration tests.test_calibration_wizard tests.test_device_topology tests.test_docking_continuity tests.test_hardware_policies -v
          python3 - <<'PY'
          import json
          manifest = json.load(open("manifest.json", encoding="utf-8"))
          assert "bar" not in manifest["kinds"]
          assert "bar-widget" in manifest["kinds"]
          assert manifest["entryPoints"]["barWidget"]
          PY
